# Реактивное веб-приложение на Django с WebSocket

## Описание проекта

Это реактивное веб-приложение на Django, которое демонстрирует работу WebSocket для синхронизации данных в реальном времени. При нажатии на кнопку счетчик увеличивается и изменения мгновенно отображаются во всех открытых вкладках браузера.

## Основные возможности

- 🔄 **Реактивное обновление** - изменения видны во всех открытых вкладках одновременно
- 💾 **Сохранение в БД** - все нажатия сохраняются в базу данных SQLite
- 🌐 **WebSocket** - использует Django Channels для двусторонней связи
- 🎨 **Минималистичный дизайн** - красивый градиентный интерфейс
- 🔒 **Атомарные операции** - защита от race conditions при одновременных кликах

## Технологии

- **Backend**: Django 4.2.7 + Django Channels 4.0.0
- **WebSocket сервер**: Daphne 4.0.0 (ASGI)
- **База данных**: SQLite
- **Frontend**: Vanilla JavaScript + HTML5 + CSS3

## Структура проекта

```
.
├── clickcounter/           # Основной проект Django
│   ├── settings.py        # Настройки проекта
│   ├── asgi.py           # ASGI конфигурация для Channels
│   └── urls.py           # URL маршруты
├── counter/               # Приложение счетчика
│   ├── models.py         # Модель ClickCounter
│   ├── views.py          # Views для отображения
│   ├── consumers.py      # WebSocket consumer
│   ├── routing.py        # WebSocket маршруты
│   ├── urls.py           # URL маршруты приложения
│   └── templates/
│       └── counter/
│           └── index.html # HTML шаблон с WebSocket клиентом
├── requirements.txt       # Python зависимости
└── manage.py             # Django management команды

```

## Инструкция для запуска в Visual Studio Code

### Шаг 1: Установка Python

1. Скачайте и установите Python 3.11 с официального сайта: https://www.python.org/downloads/
2. При установке обязательно отметьте "Add Python to PATH"
3. Проверьте установку, открыв терминал и выполнив:
   ```bash
   python --version
   ```

### Шаг 2: Клонирование проекта

1. Откройте Visual Studio Code
2. Откройте терминал (Ctrl+` или через меню View → Terminal)
3. Перейдите в папку, где хотите разместить проект
4. Скопируйте все файлы проекта в эту папку

### Шаг 3: Создание виртуального окружения

В терминале VS Code выполните:

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**macOS/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

После активации вы увидите `(venv)` в начале строки терминала.

### Шаг 4: Установка зависимостей

Установите все необходимые пакеты:

```bash
pip install -r requirements.txt
```

Это установит:
- Django 4.2.7
- channels 4.0.0
- daphne 4.0.0
- channels-redis 4.1.0
- redis 5.0.1

### Шаг 5: Применение миграций

Создайте базу данных и примените миграции:

```bash
python manage.py migrate
```

### Шаг 6: Запуск сервера

Запустите сервер разработки с поддержкой WebSocket:

```bash
daphne -b 0.0.0.0 -p 5000 clickcounter.asgi:application
```

Вы должны увидеть:
```
Starting server at tcp:port=5000:interface=0.0.0.0
Listening on TCP address 0.0.0.0:5000
```

### Шаг 7: Открытие в браузере

1. Откройте браузер
2. Перейдите по адресу: http://localhost:5000
3. Вы увидите страницу со счетчиком и кнопкой "Нажми меня!"

### Шаг 8: Тестирование реактивности

1. Откройте несколько вкладок браузера с адресом http://localhost:5000
2. Нажмите кнопку в любой вкладке
3. Счетчик обновится **во всех вкладках одновременно**!

## Как это работает

### Backend (Django)

1. **Модель ClickCounter** (`counter/models.py`):
   - Хранит текущее значение счетчика
   - Использует атомарные операции для предотвращения race conditions
   - Методы `get_current_count()` и `increment()` для работы со счетчиком

2. **WebSocket Consumer** (`counter/consumers.py`):
   - Обрабатывает WebSocket соединения
   - При подключении отправляет текущее значение счетчика
   - При получении действия "increment" увеличивает счетчик
   - Рассылает обновления всем подключенным клиентам через группу

3. **ASGI конфигурация** (`clickcounter/asgi.py`):
   - Настраивает маршрутизацию для HTTP и WebSocket
   - Инициализирует Django перед импортом приложений

### Frontend (JavaScript)

1. **WebSocket клиент** (в `index.html`):
   - Подключается к WebSocket серверу
   - Слушает обновления счетчика
   - Отправляет действие "increment" при нажатии кнопки
   - Автоматически переподключается при разрыве соединения

## Технические детали

### Атомарные операции

Метод `increment()` использует:
- `@transaction.atomic` - гарантирует атомарность операции
- `select_for_update()` - блокирует строку при чтении
- `F('count') + 1` - выполняет инкремент на уровне БД
- Это предотвращает потерю кликов при одновременных запросах

### WebSocket группы

Все WebSocket соединения объединяются в группу "counter":
- При подключении клиент присоединяется к группе
- При инкременте обновление рассылается всем в группе
- Это обеспечивает синхронизацию между вкладками

### Channel Layers

Используется InMemoryChannelLayer для обмена сообщениями между процессами:
- Для production рекомендуется использовать Redis
- Для разработки достаточно in-memory решения

## Остановка сервера

Для остановки сервера нажмите `Ctrl+C` в терминале.

## Деактивация виртуального окружения

После завершения работы деактивируйте виртуальное окружение:

```bash
deactivate
```

## Возможные проблемы и решения

### Проблема: Порт 5000 уже занят

**Решение**: Используйте другой порт:
```bash
daphne -b 0.0.0.0 -p 8000 clickcounter.asgi:application
```
И откройте http://localhost:8000

### Проблема: WebSocket не подключается

**Решение**: Убедитесь что:
1. Сервер Daphne запущен (не стандартный `runserver`)
2. В логах видно "WSCONNECT /ws/counter/"
3. Браузер не блокирует WebSocket соединения

### Проблема: Счетчик не синхронизируется

**Решение**: 
1. Откройте консоль браузера (F12)
2. Проверьте наличие ошибок
3. Убедитесь что WebSocket подключен (статус "Подключено")

## Дополнительные команды Django

### Создание суперпользователя для админки
```bash
python manage.py createsuperuser
```
Затем откройте http://localhost:5000/admin

### Просмотр миграций
```bash
python manage.py showmigrations
```

### Сброс счетчика через Django shell
```bash
python manage.py shell
```
В shell:
```python
from counter.models import ClickCounter
counter = ClickCounter.objects.get(pk=1)
counter.count = 0
counter.save()
```

## Архитектура

### Почему Django Channels?

Django по умолчанию работает в синхронном режиме и не поддерживает WebSocket. Django Channels:
- Добавляет поддержку ASGI (Asynchronous Server Gateway Interface)
- Позволяет обрабатывать WebSocket соединения
- Предоставляет механизм группировки и рассылки сообщений
- Работает с асинхронным кодом через async/await

### Почему InMemoryChannelLayer?

Для разработки используется in-memory решение:
- Простая настройка, не требует дополнительных сервисов
- Достаточно для одного процесса
- Для production с несколькими воркерами нужен Redis

### Почему SQLite?

SQLite идеально подходит для этого проекта:
- Не требует установки отдельного сервера БД
- Файловая база данных (db.sqlite3)
- Поддерживает транзакции и блокировки
- Достаточно для небольших приложений

## Следующие шаги

Если вы хотите расширить приложение:

1. **Добавить Redis для production**:
   - Установите Redis сервер
   - Измените CHANNEL_LAYERS в settings.py на Redis backend
   
2. **Добавить статистику**:
   - История всех кликов с временными метками
   - График активности по времени
   - Топ активных пользователей

3. **Добавить аутентификацию**:
   - Подсчет кликов для каждого пользователя отдельно
   - Отображение кто именно нажал кнопку

4. **Множественные счетчики**:
   - Создание разных счетчиков с уникальными ID
   - Возможность выбора счетчика

5. **Кнопка сброса**:
   - Добавить кнопку для обнуления счетчика
   - С подтверждением действия

## Полезные ссылки

- [Django документация](https://docs.djangoproject.com/)
- [Django Channels документация](https://channels.readthedocs.io/)
- [WebSocket API](https://developer.mozilla.org/en-US/docs/Web/API/WebSocket)
- [Daphne сервер](https://github.com/django/daphne)

## Лицензия

Этот проект создан в образовательных целях.
