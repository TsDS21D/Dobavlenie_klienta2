"""
urls.py - URL-маршруты (роутинг) для приложения product_templates.
Определяет, какие URL-адреса ведут к каким контроллерам (views).
"""

# Импортируем функцию path для создания URL-шаблонов
from django.urls import path

# Импортируем функции-контроллеры из файла views.py этого же приложения
# Синтаксис "from . import views" означает "импортировать из текущей директории"
from . import views

# Определяем имя приложения для использования в пространствах имен
# Это позволяет однозначно идентифицировать URL данного приложения
app_name = 'product_templates'

# Список URL-шаблонов (маршрутов) приложения
# Django проверяет этот список сверху вниз, пока не найдет совпадение
urlpatterns = [
    # Маршрут для главной страницы приложения
    # path() создает URL-шаблон с параметрами:
    #  1. '' - шаблон URL (пустая строка = корневой URL приложения)
    #  2. views.index - функция-контроллер, которая обработает запрос
    #  3. name='index' - имя маршрута для использования в шаблонах (например, {% url 'product_templates:index' %})
    path('', views.index, name='index'),
]