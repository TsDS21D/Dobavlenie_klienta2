"""
apps.py - конфигурационный файл приложения Django.
Здесь определяются настройки приложения, которые загружаются при запуске Django.
"""

# Импортируем базовый класс AppConfig из Django
from django.apps import AppConfig

# Создаем класс конфигурации приложения, наследуясь от AppConfig
class DirectoriesConfig(AppConfig):
    """
    Класс конфигурации для приложения "directories" (Справочники).
    
    Этот класс определяет:
    1. Имя приложения (как Django его идентифицирует)
    2. Настройки по умолчанию для автоматических полей в моделях
    3. Человекочитаемое имя для админ-панели
    4. Действия при запуске приложения
    """
    
    # default_auto_field определяет тип автоматического поля для первичных ключей
    # 'django.db.models.BigAutoField' - 64-битное автоинкрементное поле (большие числа)
    # Используется для совместимости с большими базами данных
    default_auto_field = 'django.db.models.BigAutoField'
    
    # name - уникальное имя приложения, которое используется:
    # - В INSTALLED_APPS в settings.py
    # - В командах управления (python manage.py migrate directories)
    # - При импорте моделей из других приложений
    name = 'directories'
    
    # verbose_name - человекочитаемое имя приложения
    # Отображается в административной панели Django и других интерфейсах
    verbose_name = 'Справочники'
    
    # Метод ready() вызывается автоматически при запуске приложения Django
    # Здесь можно выполнить инициализацию: зарегистрировать сигналы, 
    # загрузить конфигурации, выполнить проверки и т.д.
    def ready(self):
        """
        Метод инициализации приложения.
        Вызывается один раз при запуске Django.
        
        Сейчас оставляем пустым, но здесь можно:
        1. Импортировать сигналы (signals.py)
        2. Выполнить проверку настроек
        3. Зарегистрировать кастомные команды
        4. Загрузить начальные данные (fixtures)
        """
        # Пока не выполняем никаких действий при запуске
        # В будущем можно добавить инициализацию
        pass