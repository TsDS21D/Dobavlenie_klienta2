"""
urls.py - система маршрутизации (роутинг) приложения "directories".
Определяет соответствие между URL-адресами и функциями-обработчиками (views).

Django использует этот файл для определения:
- Какой обработчик вызывать для каждого URL
- Какие параметры передавать
- Какое имя присвоить маршруту для использования в шаблонах
"""

# Импортируем функцию path для создания URL-шаблонов
# path() создает маршруты с поддержкой типизированных параметров
from django.urls import path

# Импортируем все функции-обработчики из файла views.py текущего приложения
# Синтаксис "from ." означает "из текущего пакета" (директории)
from . import views

# Определяем имя приложения (app_name) для использования в пространствах имен
# Это позволяет избежать конфликтов имен маршрутов между разными приложениями
# Пример использования в шаблонах: {% url 'directories:index' %}
app_name = 'directories'

# Список URL-шаблонов приложения.
# Django проверяет этот список последовательно, сверху вниз.
# Первое совпадение определяет, какой обработчик будет вызван.
urlpatterns = [
    # Маршрут для главной страницы приложения
    # path() принимает три аргумента:
    #   1. Шаблон URL (пустая строка = корень приложения)
    #   2. Функция-обработчик (views.index)
    #   3. Имя маршрута (name='index') для использования в шаблонах
    
    # Примеры URL, которые соответствуют этому маршруту:
    # - http://localhost:8000/directories/
    # - http://localhost:8000/directories (без слэша, Django автоматически добавит)
    path('', views.index, name='index'),
    
    # В будущем здесь можно добавить другие маршруты:
    # path('devices/', views.devices_list, name='devices_list'),  # Список устройств
    # path('materials/', views.materials_list, name='materials_list'),  # Список материалов
    # path('services/', views.services_list, name='services_list'),  # Список услуг
]