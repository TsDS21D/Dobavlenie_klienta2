"""
apps.py для приложения print_price
Конфигурация приложения
"""

from django.apps import AppConfig


class PrintPriceConfig(AppConfig):
    """
    Конфигурация приложения print_price
    """
    
    # Имя приложения (должно совпадать с именем папки)
    name = 'print_price'
    
    # Человеко-читаемое имя приложения
    verbose_name = 'Расчет стоимости печати'
    
    def ready(self):
        """
        Метод вызывается при готовности приложения
        Можно использовать для регистрации сигналов и т.д.
        """
        # Пока ничего не делаем, но можно добавить сигналы
        pass