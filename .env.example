# ============================================
# НАСТРОЙКИ БАЗЫ ДАННЫХ POSTGRESQL (РАЗРАБОТКА)
# ============================================

# Имя базы данных (придумайте любое)
DB_NAME=clickcounter_dev

# Имя пользователя PostgreSQL (придумайте)
DB_USER=clickcounter_user

# Пароль пользователя (придумайте надежный пароль)
DB_PASSWORD=your_secure_password_here

# Хост (адрес сервера)
DB_HOST=localhost  # если база на этом же компьютере

# Порт PostgreSQL (по умолчанию 5432)
DB_PORT=5432

# ============================================
# НАСТРОЙКИ БЕЗОПАСНОСТИ DJANGO
# ============================================

# Секретный ключ Django (НИКОГДА не используйте в продакшене!)
# Сгенерировать новый можно командой:
# python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY='django-insecure-4kej=1toi&@-lykpnan(d7%yctg0posv6312a60k2a0v%lr&5v'

# Режим отладки (True - разработка, False - продакшен)
DEBUG=True

# Разрешенные хосты (через запятую)
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================
# ОПЦИОНАЛЬНЫЕ НАСТРОЙКИ
# ============================================

# Настройки Redis (для WebSocket)
REDIS_HOST=localhost
REDIS_PORT=6379

# Настройки почты (если нужны уведомления)
EMAIL_HOST=your_smtp_server.com
EMAIL_PORT=587
EMAIL_USER=your_email@example.com
EMAIL_PASSWORD=your_email_password