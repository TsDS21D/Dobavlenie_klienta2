"""
apps.py для приложения devices
Конфигурация приложения devices для системы управления устройствами
"""

from django.apps import AppConfig


class DevicesConfig(AppConfig):
    """
    Конфигурация приложения devices
    
    Attributes:
        default_auto_field (str): Тип поля для автоматических первичных ключей
        name (str): Имя приложения (должно совпадать с именем папки)
        verbose_name (str): Человеко-читаемое имя для админ-панели
    """
    
    # Указываем тип поля для автоматически создаваемых первичных ключей
    default_auto_field = 'django.db.models.BigAutoField'
    
    # Имя приложения (должно соответствовать имени папки приложения)
    name = 'devices'
    
    # Человеко-читаемое имя приложения (отображается в админ-панели)
    verbose_name = 'Устройства (Принтеры)'
    
    def ready(self):
        """
        Метод вызывается при готовности приложения
        Можно использовать для регистрации сигналов или других настроек
        """
        # Импортируем сигналы здесь, чтобы избежать циклических импортов
        # from . import signals
        pass