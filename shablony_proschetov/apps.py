"""
apps.py - конфигурация приложения shablony_proschetov.

Этот файл Django создаёт автоматически при выполнении команды
`python manage.py startapp shablony_proschetov`. Мы его переопределяем,
чтобы задать человекочитаемое имя приложения, которое будет отображаться
в административной панели Django.
"""

# Импортируем базовый класс AppConfig из Django.
# Все конфигурации приложений наследуются от этого класса.
from django.apps import AppConfig


class ShablonyProschetovConfig(AppConfig):
    """
    Класс конфигурации приложения "shablony_proschetov" (Справочник шаблонов просчётов).

    Задаёт:
    - тип автоинкрементного поля по умолчанию (BigAutoField — 64-битное целое),
    - уникальное имя приложения (используется в INSTALLED_APPS и в миграциях),
    - человекочитаемое имя для админки,
    - метод ready() — точку инициализации при старте Django.
    """

    # Тип поля первичного ключа по умолчанию для всех моделей приложения.
    # BigAutoField = 64-битное автоинкрементное целое (выдерживает большие объёмы).
    default_auto_field = 'django.db.models.BigAutoField'

    # Уникальное системное имя приложения — должно совпадать с именем папки.
    # Используется в INSTALLED_APPS, в командах миграций и в импортах моделей.
    name = 'shablony_proschetov'

    # Человекочитаемое имя — отображается в административной панели Django.
    verbose_name = 'Справочник шаблонов просчётов'

    def ready(self):
        """
        Метод инициализации приложения, вызывается один раз при старте Django.
        Пока оставляем пустым: здесь позже можно зарегистрировать сигналы
        или выполнить другие стартовые действия. Сейчас ничего не требуется.
        """
        pass